# Boocs Messenger API

Используй только Blueprint из `render.yaml`.

Если в Render снова появляется ошибка сборки, удали старый сервис и создай заново через:

`New +` → `Blueprint` → выбрать репозиторий.

Это создаст **Web Service** (Node), а не Static Site.
